---
import FrameworkCard from "@src/components/FrameworkCard.astro";
import Config from "@src/config/freelance.json";
---

<div
  class="mx-auto flex max-w-xl flex-wrap items-center justify-center gap-x-16 gap-y-16 lg:max-w-none"
>
  {
    Config.frameworks.map((framework) => (
      <FrameworkCard
        name={framework.name}
        logo={framework.logo}
        url={new URL(framework.url)}
      />
    ))
  }
</div>
