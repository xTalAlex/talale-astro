---
import * as mdDoc from "@src/data/markdown/iubenda-partner.md";
import Layout from "@src/layouts/Layout.astro";

const Content = mdDoc.Content;
---

<Layout
  title={mdDoc.frontmatter.title}
  description={mdDoc.frontmatter.description}
>
  <div class="prose prose-lg container w-full max-w-5xl px-5 pt-12 md:mx-auto">
    <Content />
  </div>
</Layout>
