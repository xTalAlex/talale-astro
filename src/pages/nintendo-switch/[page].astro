---
import Layout from "../../layouts/Layout.astro";
import { games } from "../../data/switch-games";

export async function getStaticPaths({ paginate }) {
    return paginate(games, { pageSize: 30 });
}
const { page } = Astro.props;

// console.log(page.data.filter(game => game.status != 0 ).length);
// console.log(page.data.map(game => game.first_release_date ));
---

<Layout>

    <div class="px-5 md:mx-auto container max-w-5xl">
        <h1 class="text-center py-10 text-4xl font-semibold">Nintendo Switch | Pagina {page.currentPage}</h1>

        <div class="mt-12 mx-auto grid sm:grid-cols-2 lg:grid-cols-3 place-items-center h-max gap-6">
            {
                page.data.map( (game) => (
                <div class="card card-compact w-full bg-base-100 shadow-xl h-full">
                    {
                        (game.cover || game.screenshots) && 
                            <figure>
                                <div class="swiper h-64 w-full">
                                    <div class="swiper-wrapper">
                                    {
                                        game.cover &&
                                        <div class="swiper-slide">
                                            <img  class="h-64 w-full object-cover" src={game.cover.url.replace('t_thumb','t_cover_big')} alt={game.name} />
                                        </div>
                                    }
                                    {
                                        game.screenshots && game.screenshots.map( (screenshot,id) => (
                                            <div class="swiper-slide">
                                                <img class="h-full w-full object-cover" src={screenshot.url.replace('t_thumb','t_screenshot_big')} />
                                            </div> 
                                        ))
                                    }
                                    </div>
                                    <div class="swiper-pagination"></div>
                                    <div class="swiper-button-prev"></div>
                                    <div class="swiper-button-next"></div>
                                </div>
                            </figure>
                    }
                    <div class="card-body">
                        <h2 class="card-title" id={game.name}>
                            {
                                game.websites && <a href={game.websites[0].url} class="link l" target="_blank">{game.name}</a>
                            }
                            {
                                !game.websites && <span>{game.name}</span>
                            }
                        </h2>
                        <div class="w-full flex justify-between">
                            <div>
                                {
                                    game.platforms?.length==1 && <div class="badge badge-accent">Exclusive</div>
                                }
                            </div>
                            {
                                game.total_rating &&
                                <div class="badge badge-primary">{Math.round(game.total_rating)}</div>
                            }
                            {   
                                game.first_release_date &&
                                <div class="badge badge-ghost">{new Date(game.first_release_date * 1000).toLocaleDateString()}</div> 
                            }    
                        </div>
                        <p class="h-32 overflow-y-auto my-6">{game.summary}</p>
                        <div class="flex flex-col space-y-1">
                            <div class="w-full  flex flex-nowrap space-x-2 h-12 overflow-x-auto overflow-y-hidden">
                                {   
                                    game.genres?.map( (genre) => (
                                        <div class="badge whitespace-nowrap badge-primary">{genre.name}</div>
                                    ))
                                }
                            </div>
                            <div class="w-full flex flex-nowrap space-x-2 h-12 overflow-x-auto overflow-y-hidden">
                                {   
                                    game.themes?.map( (theme) => (
                                        <div class="badge whitespace-nowrap badge-secondary">{theme.name}</div>
                                    ))
                                }
                            </div>
                            <div class="flex space-x-2 h-12 overflow-x-auto justify-end overflow-y-hidden">
                                {   
                                    game.involved_companies?.map( (company) => company.company.logo && company.company.websites && (
                                        <a href={company.company.websites && company.company.websites[0].url} target="_blank">
                                            <img class="h-10 object-fill rounded-full" alt={company.company.name} src={company.company.logo && company.company.logo.url}/>
                                        </a>
                                    ))
                                }
                            </div>    
                        </div>
                    </div>
                </div>
                ))
            }
        </div>

        <div class="divider"></div>

        <div class="flex flex-col sm:flex-row sm:justify-between space-y-4 sm:space-y-0">
            { page.url.prev ?
                <a href={page.url.prev} class="btn btn-ghost justify-start sm:space-x-2 w-full sm:w-auto">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5" />
                    </svg>
                    <span class="link whitespace-nowrap overflow-hidden w-48 sm:w-32 lg:w-64 overflow-ellipsis" >Prec.</span>
                </a>
                :
                <div></div>
            }
            { page.url.next ?
                <a  href={page.url.next} class="btn btn-ghost justify-end sm:space-x-2 w-full sm:w-auto">
                    <span class="link whitespace-nowrap overflow-hidden w-48 sm:w-32 lg:w-64 overflow-ellipsis">Succ.</span>
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
                    </svg>
                </a>
                :
                <div></div>
            }
        </div>
    </div>

    <script>
        import Swiper, { Navigation, Pagination } from "swiper";
        const swipers = new Swiper(".swiper", {
            modules: [Navigation, Pagination ],
            loop: true,
            pagination: {
                el: ".swiper-pagination",
            },
            navigation: {
                nextEl: ".swiper-button-next",
                prevEl: ".swiper-button-prev",
            },
        });
    </script>
    
</Layout>