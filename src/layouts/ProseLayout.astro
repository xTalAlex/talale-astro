---
import Layout from "./Layout.astro";

export interface Props {
  title?: string;
  description?: string;
  ogType?: string;
  ogImage?: string;
  ogImageAlt?: string;
  noindex?: boolean;
}

const {
  title,
  description,
  ogType,
  ogImage,
  ogImageAlt,
  noindex = false,
} = Astro.props.frontmatter || Astro.props;
---

<Layout
  title={title}
  description={description}
  ogType={ogType}
  ogImage={ogImage}
  ogImageAlt={ogImageAlt}
  noindex={noindex}
>
  <div class="card">
    <div
      class="prose card-body prose-a:break-all prose-figure:flex-col prose-code:overflow-scroll prose-code:wrap-break-word container mx-auto px-10 lg:max-w-4xl"
    >
      <slot />
    </div>
  </div>
</Layout>

<script>
  const codeTags = [...document.getElementsByTagName("code")];
  codeTags.forEach((code) => {
    const aTags = [...code.getElementsByTagName("a")];
    aTags.forEach((a) => {
      a.classList.add("link", "link-secondary");
    });
  });
</script>
